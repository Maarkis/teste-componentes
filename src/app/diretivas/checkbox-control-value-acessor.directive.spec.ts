import { CheckboxControlValueAcessorDirective } from './checkbox-control-value-acessor.directive';

describe('CheckboxControlValueAcessorDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckboxControlValueAcessorDirective();
    expect(directive).toBeTruthy();
  });
});
